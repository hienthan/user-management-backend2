# ============================================
# Backend Environment Variables (Docker)
# ============================================

# --- Application Settings ---
NODE_ENV=development
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=debug

# --- Security ---
# Generate a secure key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
APP_KEY=k-_5ZZQ7glzSELyKyMnT6TuVTgmTJmnP

# --- Database Connection ---
# Since PostgreSQL runs in a SEPARATE docker-compose, you need to connect via host
# Option 1: host.docker.internal (Docker Desktop on Mac/Windows)
# Option 2: 172.17.0.1 (Docker default bridge gateway on Linux)
# On a user-defined bridge network (app_net), the gateway is not 172.17.0.1
# Option 3: Your actual machine IP

DB_HOST=host.docker.internal   # correct host for containers on app_net
DB_PORT=5433                   # matches db compose 5433:5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=testdb